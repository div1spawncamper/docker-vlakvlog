<h1>Posts</h1>

El blog tiene <%= Article.public_count %> posts.

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
  <% end %>
</ul>

<%= link_to "Nuevo Post", new_article_path %>
